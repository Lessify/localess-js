---
import { localessEditable } from '@localess/astro';
import type { Page } from '../shared/models/localess';

export interface Props {
  data: Page;
}

const { data } = Astro.props;
---

<main {...localessEditable(data)}>
  <h1>{data.title}</h1>
</main>
