/**
 * Connection settings for the shared Localess demo space — replace with your own.
 *
 * Kept in its own module because the static playground's `vite.config.ts` imports it too,
 * to enumerate prerender routes at build time.
 */
export const LOCALESS_CONNECTION = {
  origin: 'https://demo.localess.org', // Replace it for your origin
  spaceId: 'MmaT4DL0kJ6nXIILUcQF', // Replace it for your spaceId
  token: 'Y4rvboPnyzVeC7LddEK5', // Replace it for your Public token
};
